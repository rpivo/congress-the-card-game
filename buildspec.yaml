version: 0.2

phases:
  install:
    commands:
      - echo installing packages...
      - npm install
  build:
    commands:
      - echo running tests...
      - npx test
      - echo building...
      - npm run build

artifacts:
  files:
    - dist/*
  discard-paths: yes