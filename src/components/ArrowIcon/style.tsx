import styled from 'styled-components';

const Style = styled.div`
  cursor: pointer;

  &.endTurnIcon {
    display: inline-block;
    position: fixed;
    right: 22px;
    top: 22px;
  }
`;

export default Style;
